<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="llamadas.list">
      <field name="name">llamadas list</field>
      <field name="model">llamadas.llamadas</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="llamadas.action_window">
      <field name="name">llamadas window</field>
      <field name="res_model">llamadas.llamadas</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="llamadas.action_server">
      <field name="name">llamadas server</field>
      <field name="model_id" ref="model_llamadas_llamadas"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="llamadas" id="llamadas.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="llamadas.menu_1" parent="llamadas.menu_root"/>
    <menuitem name="Menu 2" id="llamadas.menu_2" parent="llamadas.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="llamadas.menu_1_list" parent="llamadas.menu_1"
              action="llamadas.action_window"/>
    <menuitem name="Server to list" id="llamadas" parent="llamadas.menu_2"
              action="llamadas.action_server"/>
    -->
  </data>
</odoo>